# `@bbqbros/config`

Validación tipada y explícita de configuración. Cada aplicación declarará las variables que necesita
y fallará al iniciar si falta una variable obligatoria.

Este paquete define nombres y reglas; nunca contiene valores reales.
